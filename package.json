{
  "name": "health-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "start": "lerna run start",
    "dev": "lerna run dev"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
